name: Apply GPS Attendance Security Fix

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual-only workflow retained for audit history; use Repair GPS Attendance Security for the automated repair."