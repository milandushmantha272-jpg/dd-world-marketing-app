import React from 'react';
import { SalesSummaryPage } from '../../common/SalesSummaryPage';

/** Compatibility entry point for the existing Owner dashboard route. */
export const ExecutiveSummariesPage: React.FC = () => <SalesSummaryPage />;
